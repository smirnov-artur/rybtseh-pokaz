/*!
 * РЫБЦЕХ — номер счётчика Яндекс.Метрики, ЕДИНСТВЕННОЕ место для правки.
 * Подключается ДО assets/metrika.js на каждой странице сайта.
 *
 * Пока владелец не дал номер — здесь плейсхолдер 'XXXXXXX', metrika.js
 * при этом виде номера сам ничего не грузит и не отправляет (см.
 * КОМПОНЕНТЫ/analytics/README.md) — сайт не ломается, это не баг.
 *
 * Когда номер появится: вписать восьмизначное число ОДИН раз сюда —
 * ссылку на assets/metrika.js в семи HTML-файлах трогать не нужно.
 * (Кроме одной статической мелочи: <noscript><img src="…/watch/XXXXXXX">
 * во всех семи файлах — она отдаётся без JS и не может прочитать это
 * значение, поэтому её придётся поправить тем же числом отдельно —
 * см. пометку рядом с каждым <noscript> на страницах.)
 */
window.RYBTSEH_METRIKA = window.RYBTSEH_METRIKA || { id: 'XXXXXXX' };
