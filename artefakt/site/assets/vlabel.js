/*!
 * РЫБЦЕХ — «Артефакт»: .vlabel как перетаскиваемая полоса прокрутки.
 * Логика перенесена в новый модуль в конце assets/artefakt.js (задание —
 * править только artefakt.css/artefakt.js). Этот файл оставлен пустым
 * заглушкой, потому что <script src="assets/vlabel.js"> уже подключён на
 * всех страницах — если восстановить здесь код, обработчики продублируются
 * и, например, стрелки будут прокручивать страницу вдвое быстрее.
 */
