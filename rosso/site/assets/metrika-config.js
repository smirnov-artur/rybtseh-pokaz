/*!
 * РЫБЦЕХ — номер счётчика Яндекс.Метрики для «Россо».
 * ЕДИНСТВЕННОЕ место, где его нужно вписать: заменить null ниже на номер
 * счётчика (восьмизначное число, взять на https://metrika.yandex.ru/ —
 * «Добавить счётчик» → страница настроек счётчика, номер в самом верху).
 * Этот файл подключён на всех семи страницах ДО assets/metrika.js — правка
 * здесь одна, применяется сразу везде, семь HTML-файлов трогать не нужно.
 *
 * Пока стоит null — metrika.js видит пустой id, значит счётчик остаётся
 * плейсхолдером и молчит (см. КОМПОНЕНТЫ/analytics/README.md, раздел 1):
 * ничего никуда не отправляет, сайт не ломается.
 */
window.RYBTSEH_METRIKA = { id: null };
